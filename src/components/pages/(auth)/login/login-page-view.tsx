import { LoginForm } from './subcomponents/form/login-form';

export const LoginPageView = () => (
  <div className="flex h-screen w-screen items-center justify-center">
    <LoginForm />
  </div>
);
